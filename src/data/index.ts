import {
  Item,
  earthEphemerals,
  earthLegendaries,
  fireEphemerals,
  fireLegendaries,
  iceEphemerals,
  iceLegendaries,
  lightningEphemerals,
  lightningLegendaries,
  waterEphemerals,
  waterLegendaries,
  windEphemerals,
  windLegendaries,
} from "./data";

export type { Item };

export const earth = {
  ephemerals: earthEphemerals,
  legendaries: earthLegendaries,
};

export const fire = {
  ephemerals: fireEphemerals,
  legendaries: fireLegendaries,
};

export const ice = {
  ephemerals: iceEphemerals,
  legendaries: iceLegendaries,
};

export const lightning = {
  ephemerals: lightningEphemerals,
  legendaries: lightningLegendaries,
};

export const water = {
  ephemerals: waterEphemerals,
  legendaries: waterLegendaries,
};

export const wind = {
  ephemerals: windEphemerals,
  legendaries: windLegendaries,
};
